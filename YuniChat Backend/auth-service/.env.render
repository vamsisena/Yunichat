# Render Environment Variables for auth-service
# Copy these to Render Dashboard → yunichat-auth → Environment

# Database Configuration (use FULL URL with jdbc: prefix and full hostname)
SPRING_DATASOURCE_URL=jdbc:postgresql://dpg-d4q2il2dbo4c73bkg6q0-a.singapore-postgres.render.com:5432/yunichat
SPRING_DATASOURCE_USERNAME=yunichat_user
SPRING_DATASOURCE_PASSWORD=lgq3oVGWhC17e4to2q8q4YJvcg9AoDIq

# JWT Configuration (REQUIRED - MUST match across ALL services)
JWT_SECRET=yunichat-jwt-secret-2024-prod-key-abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
JWT_EXPIRATION=3600000
JWT_REFRESH_EXPIRATION=86400000

# Mail Configuration (Optional - for email verification)
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# Redis Configuration (Optional - disabled in code)
# SPRING_REDIS_HOST=your-redis-host
# SPRING_REDIS_PORT=6379

# Kafka Configuration (Optional - disabled in code)
# SPRING_KAFKA_BOOTSTRAP_SERVERS=your-kafka-server:9092
